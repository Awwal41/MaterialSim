# MaterialSim AI Agent - Core Requirements
# These are everything needed to run real MD simulations, analysis, the CLI,
# and the web GUI. Optional heavy extras (database/ML) live in
# requirements-optional.txt.

# LLM orchestration (optional conversational chat; core features work without a key)
langchain>=1.0,<2.0
langchain-core>=1.0,<2.0
langchain-openai>=1.0,<2.0
langgraph>=0.2.0
python-dotenv>=1.0.0

# Scientific computing and data analysis
numpy>=1.24.3
scipy>=1.11.4
pandas>=2.0.3
matplotlib>=3.7.2

# Materials science and molecular dynamics (real MD engine)
ase>=3.22.1

# Configuration and utilities
pydantic>=2.5.0
pyyaml>=6.0.1
tqdm>=4.66.1

# Web interface
streamlit>=1.28.0
